package co.istad.dealkh.features.image.dto;

/**
 * ImageResponse is a response object for an image.
 * It contains the URL of the image.
 *
 * @param url
 */
public record ImageResponse(
        String url
) {
}
